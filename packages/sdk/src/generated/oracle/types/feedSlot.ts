/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";
import {
  getFeedKindDecoder,
  getFeedKindEncoder,
  type FeedKind,
  type FeedKindArgs,
} from ".";

/** A configured feed slot: which adapter kind and which on-chain account it reads. */
export type FeedSlot = {
  /** Adapter kind for this slot. */
  kind: FeedKind;
  /** The price/source account the adapter reads (e.g. Pyth price account). */
  account: Address;
};

export type FeedSlotArgs = {
  /** Adapter kind for this slot. */
  kind: FeedKindArgs;
  /** The price/source account the adapter reads (e.g. Pyth price account). */
  account: Address;
};

export function getFeedSlotEncoder(): FixedSizeEncoder<FeedSlotArgs> {
  return getStructEncoder([
    ["kind", getFeedKindEncoder()],
    ["account", getAddressEncoder()],
  ]);
}

export function getFeedSlotDecoder(): FixedSizeDecoder<FeedSlot> {
  return getStructDecoder([
    ["kind", getFeedKindDecoder()],
    ["account", getAddressDecoder()],
  ]);
}

export function getFeedSlotCodec(): FixedSizeCodec<FeedSlotArgs, FeedSlot> {
  return combineCodec(getFeedSlotEncoder(), getFeedSlotDecoder());
}
