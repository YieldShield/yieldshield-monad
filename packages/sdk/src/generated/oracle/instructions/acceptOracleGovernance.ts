/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findOracleConfigPda } from "../pdas";
import { COMPOSITE_ORACLE_PROGRAM_ADDRESS } from "../programs";

export const ACCEPT_ORACLE_GOVERNANCE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([122, 235, 95, 252, 21, 42, 70, 16]);

export function getAcceptOracleGovernanceDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ACCEPT_ORACLE_GOVERNANCE_DISCRIMINATOR,
  );
}

export type AcceptOracleGovernanceInstruction<
  TProgram extends string = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
  TAccountPendingAuthority extends string | AccountMeta<string> = string,
  TAccountOracleConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPendingAuthority extends string
        ? ReadonlySignerAccount<TAccountPendingAuthority> &
            AccountSignerMeta<TAccountPendingAuthority>
        : TAccountPendingAuthority,
      TAccountOracleConfig extends string
        ? WritableAccount<TAccountOracleConfig>
        : TAccountOracleConfig,
      ...TRemainingAccounts,
    ]
  >;

export type AcceptOracleGovernanceInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AcceptOracleGovernanceInstructionDataArgs = {};

export function getAcceptOracleGovernanceInstructionDataEncoder(): FixedSizeEncoder<AcceptOracleGovernanceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: ACCEPT_ORACLE_GOVERNANCE_DISCRIMINATOR,
    }),
  );
}

export function getAcceptOracleGovernanceInstructionDataDecoder(): FixedSizeDecoder<AcceptOracleGovernanceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAcceptOracleGovernanceInstructionDataCodec(): FixedSizeCodec<
  AcceptOracleGovernanceInstructionDataArgs,
  AcceptOracleGovernanceInstructionData
> {
  return combineCodec(
    getAcceptOracleGovernanceInstructionDataEncoder(),
    getAcceptOracleGovernanceInstructionDataDecoder(),
  );
}

export type AcceptOracleGovernanceAsyncInput<
  TAccountPendingAuthority extends string = string,
  TAccountOracleConfig extends string = string,
> = {
  pendingAuthority: TransactionSigner<TAccountPendingAuthority>;
  oracleConfig?: Address<TAccountOracleConfig>;
};

export async function getAcceptOracleGovernanceInstructionAsync<
  TAccountPendingAuthority extends string,
  TAccountOracleConfig extends string,
  TProgramAddress extends Address = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
>(
  input: AcceptOracleGovernanceAsyncInput<
    TAccountPendingAuthority,
    TAccountOracleConfig
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AcceptOracleGovernanceInstruction<
    TProgramAddress,
    TAccountPendingAuthority,
    TAccountOracleConfig
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPOSITE_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pendingAuthority: {
      value: input.pendingAuthority ?? null,
      isWritable: false,
    },
    oracleConfig: { value: input.oracleConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.oracleConfig.value) {
    accounts.oracleConfig.value = await findOracleConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pendingAuthority", accounts.pendingAuthority),
      getAccountMeta("oracleConfig", accounts.oracleConfig),
    ],
    data: getAcceptOracleGovernanceInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptOracleGovernanceInstruction<
    TProgramAddress,
    TAccountPendingAuthority,
    TAccountOracleConfig
  >);
}

export type AcceptOracleGovernanceInput<
  TAccountPendingAuthority extends string = string,
  TAccountOracleConfig extends string = string,
> = {
  pendingAuthority: TransactionSigner<TAccountPendingAuthority>;
  oracleConfig: Address<TAccountOracleConfig>;
};

export function getAcceptOracleGovernanceInstruction<
  TAccountPendingAuthority extends string,
  TAccountOracleConfig extends string,
  TProgramAddress extends Address = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
>(
  input: AcceptOracleGovernanceInput<
    TAccountPendingAuthority,
    TAccountOracleConfig
  >,
  config?: { programAddress?: TProgramAddress },
): AcceptOracleGovernanceInstruction<
  TProgramAddress,
  TAccountPendingAuthority,
  TAccountOracleConfig
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPOSITE_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    pendingAuthority: {
      value: input.pendingAuthority ?? null,
      isWritable: false,
    },
    oracleConfig: { value: input.oracleConfig ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("pendingAuthority", accounts.pendingAuthority),
      getAccountMeta("oracleConfig", accounts.oracleConfig),
    ],
    data: getAcceptOracleGovernanceInstructionDataEncoder().encode({}),
    programAddress,
  } as AcceptOracleGovernanceInstruction<
    TProgramAddress,
    TAccountPendingAuthority,
    TAccountOracleConfig
  >);
}

export type ParsedAcceptOracleGovernanceInstruction<
  TProgram extends string = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    pendingAuthority: TAccountMetas[0];
    oracleConfig: TAccountMetas[1];
  };
  data: AcceptOracleGovernanceInstructionData;
};

export function parseAcceptOracleGovernanceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAcceptOracleGovernanceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      pendingAuthority: getNextAccount(),
      oracleConfig: getNextAccount(),
    },
    data: getAcceptOracleGovernanceInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
