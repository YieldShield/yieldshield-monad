/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findFeedPda } from "../pdas";
import { COMPOSITE_ORACLE_PROGRAM_ADDRESS } from "../programs";

export const GET_VALUE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  88, 79, 40, 100, 21, 74, 119, 125,
]);

export function getGetValueDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GET_VALUE_DISCRIMINATOR);
}

export type GetValueInstruction<
  TProgram extends string = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
  TAccountFeed extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFeed extends string
        ? ReadonlyAccount<TAccountFeed>
        : TAccountFeed,
      ...TRemainingAccounts,
    ]
  >;

export type GetValueInstructionData = {
  discriminator: ReadonlyUint8Array;
  tokenMint: Address;
  amount: bigint;
  decimals: number;
};

export type GetValueInstructionDataArgs = {
  tokenMint: Address;
  amount: number | bigint;
  decimals: number;
};

export function getGetValueInstructionDataEncoder(): FixedSizeEncoder<GetValueInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tokenMint", getAddressEncoder()],
      ["amount", getU128Encoder()],
      ["decimals", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: GET_VALUE_DISCRIMINATOR }),
  );
}

export function getGetValueInstructionDataDecoder(): FixedSizeDecoder<GetValueInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tokenMint", getAddressDecoder()],
    ["amount", getU128Decoder()],
    ["decimals", getU8Decoder()],
  ]);
}

export function getGetValueInstructionDataCodec(): FixedSizeCodec<
  GetValueInstructionDataArgs,
  GetValueInstructionData
> {
  return combineCodec(
    getGetValueInstructionDataEncoder(),
    getGetValueInstructionDataDecoder(),
  );
}

export type GetValueAsyncInput<TAccountFeed extends string = string> = {
  feed?: Address<TAccountFeed>;
  tokenMint: GetValueInstructionDataArgs["tokenMint"];
  amount: GetValueInstructionDataArgs["amount"];
  decimals: GetValueInstructionDataArgs["decimals"];
};

export async function getGetValueInstructionAsync<
  TAccountFeed extends string,
  TProgramAddress extends Address = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
>(
  input: GetValueAsyncInput<TAccountFeed>,
  config?: { programAddress?: TProgramAddress },
): Promise<GetValueInstruction<TProgramAddress, TAccountFeed>> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPOSITE_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    feed: { value: input.feed ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.feed.value) {
    accounts.feed.value = await findFeedPda({
      tokenMint: getNonNullResolvedInstructionInput(
        "tokenMint",
        args.tokenMint,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("feed", accounts.feed)],
    data: getGetValueInstructionDataEncoder().encode(
      args as GetValueInstructionDataArgs,
    ),
    programAddress,
  } as GetValueInstruction<TProgramAddress, TAccountFeed>);
}

export type GetValueInput<TAccountFeed extends string = string> = {
  feed: Address<TAccountFeed>;
  tokenMint: GetValueInstructionDataArgs["tokenMint"];
  amount: GetValueInstructionDataArgs["amount"];
  decimals: GetValueInstructionDataArgs["decimals"];
};

export function getGetValueInstruction<
  TAccountFeed extends string,
  TProgramAddress extends Address = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
>(
  input: GetValueInput<TAccountFeed>,
  config?: { programAddress?: TProgramAddress },
): GetValueInstruction<TProgramAddress, TAccountFeed> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPOSITE_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    feed: { value: input.feed ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("feed", accounts.feed)],
    data: getGetValueInstructionDataEncoder().encode(
      args as GetValueInstructionDataArgs,
    ),
    programAddress,
  } as GetValueInstruction<TProgramAddress, TAccountFeed>);
}

export type ParsedGetValueInstruction<
  TProgram extends string = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    feed: TAccountMetas[0];
  };
  data: GetValueInstructionData;
};

export function parseGetValueInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGetValueInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { feed: getNextAccount() },
    data: getGetValueInstructionDataDecoder().decode(instruction.data),
  };
}
