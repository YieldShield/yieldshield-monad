/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findFeedPda, findOracleConfigPda } from "../pdas";
import { COMPOSITE_ORACLE_PROGRAM_ADDRESS } from "../programs";

export const PUSH_MANUAL_PRICE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([53, 111, 35, 196, 190, 70, 183, 10]);

export function getPushManualPriceDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PUSH_MANUAL_PRICE_DISCRIMINATOR,
  );
}

export type PushManualPriceInstruction<
  TProgram extends string = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
  TAccountGovernanceAuthority extends string | AccountMeta<string> = string,
  TAccountOracleConfig extends string | AccountMeta<string> = string,
  TAccountFeed extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGovernanceAuthority extends string
        ? ReadonlySignerAccount<TAccountGovernanceAuthority> &
            AccountSignerMeta<TAccountGovernanceAuthority>
        : TAccountGovernanceAuthority,
      TAccountOracleConfig extends string
        ? ReadonlyAccount<TAccountOracleConfig>
        : TAccountOracleConfig,
      TAccountFeed extends string
        ? WritableAccount<TAccountFeed>
        : TAccountFeed,
      ...TRemainingAccounts,
    ]
  >;

export type PushManualPriceInstructionData = {
  discriminator: ReadonlyUint8Array;
  tokenMint: Address;
  isBackup: boolean;
  price: bigint;
  publishTime: bigint;
};

export type PushManualPriceInstructionDataArgs = {
  tokenMint: Address;
  isBackup: boolean;
  price: number | bigint;
  publishTime: number | bigint;
};

export function getPushManualPriceInstructionDataEncoder(): FixedSizeEncoder<PushManualPriceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tokenMint", getAddressEncoder()],
      ["isBackup", getBooleanEncoder()],
      ["price", getU128Encoder()],
      ["publishTime", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PUSH_MANUAL_PRICE_DISCRIMINATOR }),
  );
}

export function getPushManualPriceInstructionDataDecoder(): FixedSizeDecoder<PushManualPriceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tokenMint", getAddressDecoder()],
    ["isBackup", getBooleanDecoder()],
    ["price", getU128Decoder()],
    ["publishTime", getI64Decoder()],
  ]);
}

export function getPushManualPriceInstructionDataCodec(): FixedSizeCodec<
  PushManualPriceInstructionDataArgs,
  PushManualPriceInstructionData
> {
  return combineCodec(
    getPushManualPriceInstructionDataEncoder(),
    getPushManualPriceInstructionDataDecoder(),
  );
}

export type PushManualPriceAsyncInput<
  TAccountGovernanceAuthority extends string = string,
  TAccountOracleConfig extends string = string,
  TAccountFeed extends string = string,
> = {
  governanceAuthority: TransactionSigner<TAccountGovernanceAuthority>;
  oracleConfig?: Address<TAccountOracleConfig>;
  feed?: Address<TAccountFeed>;
  tokenMint: PushManualPriceInstructionDataArgs["tokenMint"];
  isBackup: PushManualPriceInstructionDataArgs["isBackup"];
  price: PushManualPriceInstructionDataArgs["price"];
  publishTime: PushManualPriceInstructionDataArgs["publishTime"];
};

export async function getPushManualPriceInstructionAsync<
  TAccountGovernanceAuthority extends string,
  TAccountOracleConfig extends string,
  TAccountFeed extends string,
  TProgramAddress extends Address = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
>(
  input: PushManualPriceAsyncInput<
    TAccountGovernanceAuthority,
    TAccountOracleConfig,
    TAccountFeed
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  PushManualPriceInstruction<
    TProgramAddress,
    TAccountGovernanceAuthority,
    TAccountOracleConfig,
    TAccountFeed
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPOSITE_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    governanceAuthority: {
      value: input.governanceAuthority ?? null,
      isWritable: false,
    },
    oracleConfig: { value: input.oracleConfig ?? null, isWritable: false },
    feed: { value: input.feed ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.oracleConfig.value) {
    accounts.oracleConfig.value = await findOracleConfigPda();
  }
  if (!accounts.feed.value) {
    accounts.feed.value = await findFeedPda({
      tokenMint: getNonNullResolvedInstructionInput(
        "tokenMint",
        args.tokenMint,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("governanceAuthority", accounts.governanceAuthority),
      getAccountMeta("oracleConfig", accounts.oracleConfig),
      getAccountMeta("feed", accounts.feed),
    ],
    data: getPushManualPriceInstructionDataEncoder().encode(
      args as PushManualPriceInstructionDataArgs,
    ),
    programAddress,
  } as PushManualPriceInstruction<
    TProgramAddress,
    TAccountGovernanceAuthority,
    TAccountOracleConfig,
    TAccountFeed
  >);
}

export type PushManualPriceInput<
  TAccountGovernanceAuthority extends string = string,
  TAccountOracleConfig extends string = string,
  TAccountFeed extends string = string,
> = {
  governanceAuthority: TransactionSigner<TAccountGovernanceAuthority>;
  oracleConfig: Address<TAccountOracleConfig>;
  feed: Address<TAccountFeed>;
  tokenMint: PushManualPriceInstructionDataArgs["tokenMint"];
  isBackup: PushManualPriceInstructionDataArgs["isBackup"];
  price: PushManualPriceInstructionDataArgs["price"];
  publishTime: PushManualPriceInstructionDataArgs["publishTime"];
};

export function getPushManualPriceInstruction<
  TAccountGovernanceAuthority extends string,
  TAccountOracleConfig extends string,
  TAccountFeed extends string,
  TProgramAddress extends Address = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
>(
  input: PushManualPriceInput<
    TAccountGovernanceAuthority,
    TAccountOracleConfig,
    TAccountFeed
  >,
  config?: { programAddress?: TProgramAddress },
): PushManualPriceInstruction<
  TProgramAddress,
  TAccountGovernanceAuthority,
  TAccountOracleConfig,
  TAccountFeed
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPOSITE_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    governanceAuthority: {
      value: input.governanceAuthority ?? null,
      isWritable: false,
    },
    oracleConfig: { value: input.oracleConfig ?? null, isWritable: false },
    feed: { value: input.feed ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("governanceAuthority", accounts.governanceAuthority),
      getAccountMeta("oracleConfig", accounts.oracleConfig),
      getAccountMeta("feed", accounts.feed),
    ],
    data: getPushManualPriceInstructionDataEncoder().encode(
      args as PushManualPriceInstructionDataArgs,
    ),
    programAddress,
  } as PushManualPriceInstruction<
    TProgramAddress,
    TAccountGovernanceAuthority,
    TAccountOracleConfig,
    TAccountFeed
  >);
}

export type ParsedPushManualPriceInstruction<
  TProgram extends string = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    governanceAuthority: TAccountMetas[0];
    oracleConfig: TAccountMetas[1];
    feed: TAccountMetas[2];
  };
  data: PushManualPriceInstructionData;
};

export function parsePushManualPriceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPushManualPriceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      governanceAuthority: getNextAccount(),
      oracleConfig: getNextAccount(),
      feed: getNextAccount(),
    },
    data: getPushManualPriceInstructionDataDecoder().decode(instruction.data),
  };
}
