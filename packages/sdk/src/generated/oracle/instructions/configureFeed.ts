/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findOracleConfigPda } from "../pdas";
import { COMPOSITE_ORACLE_PROGRAM_ADDRESS } from "../programs";
import {
  getFeedSlotDecoder,
  getFeedSlotEncoder,
  type FeedSlot,
  type FeedSlotArgs,
} from "../types";

export const CONFIGURE_FEED_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  148, 186, 157, 93, 231, 55, 200, 6,
]);

export function getConfigureFeedDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CONFIGURE_FEED_DISCRIMINATOR,
  );
}

export type ConfigureFeedInstruction<
  TProgram extends string = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
  TAccountGovernanceAuthority extends string | AccountMeta<string> = string,
  TAccountOracleConfig extends string | AccountMeta<string> = string,
  TAccountFeed extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGovernanceAuthority extends string
        ? WritableSignerAccount<TAccountGovernanceAuthority> &
            AccountSignerMeta<TAccountGovernanceAuthority>
        : TAccountGovernanceAuthority,
      TAccountOracleConfig extends string
        ? ReadonlyAccount<TAccountOracleConfig>
        : TAccountOracleConfig,
      TAccountFeed extends string
        ? WritableAccount<TAccountFeed>
        : TAccountFeed,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ConfigureFeedInstructionData = {
  discriminator: ReadonlyUint8Array;
  tokenMint: Address;
  primary: FeedSlot;
  backup: Option<FeedSlot>;
  requiresStrict: boolean;
  maxPriceAge: bigint;
  maxConfidenceBps: bigint;
  maxDeviationBps: bigint;
};

export type ConfigureFeedInstructionDataArgs = {
  tokenMint: Address;
  primary: FeedSlotArgs;
  backup: OptionOrNullable<FeedSlotArgs>;
  requiresStrict: boolean;
  maxPriceAge: number | bigint;
  maxConfidenceBps: number | bigint;
  maxDeviationBps: number | bigint;
};

export function getConfigureFeedInstructionDataEncoder(): Encoder<ConfigureFeedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tokenMint", getAddressEncoder()],
      ["primary", getFeedSlotEncoder()],
      ["backup", getOptionEncoder(getFeedSlotEncoder())],
      ["requiresStrict", getBooleanEncoder()],
      ["maxPriceAge", getU64Encoder()],
      ["maxConfidenceBps", getU64Encoder()],
      ["maxDeviationBps", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CONFIGURE_FEED_DISCRIMINATOR }),
  );
}

export function getConfigureFeedInstructionDataDecoder(): Decoder<ConfigureFeedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tokenMint", getAddressDecoder()],
    ["primary", getFeedSlotDecoder()],
    ["backup", getOptionDecoder(getFeedSlotDecoder())],
    ["requiresStrict", getBooleanDecoder()],
    ["maxPriceAge", getU64Decoder()],
    ["maxConfidenceBps", getU64Decoder()],
    ["maxDeviationBps", getU64Decoder()],
  ]);
}

export function getConfigureFeedInstructionDataCodec(): Codec<
  ConfigureFeedInstructionDataArgs,
  ConfigureFeedInstructionData
> {
  return combineCodec(
    getConfigureFeedInstructionDataEncoder(),
    getConfigureFeedInstructionDataDecoder(),
  );
}

export type ConfigureFeedAsyncInput<
  TAccountGovernanceAuthority extends string = string,
  TAccountOracleConfig extends string = string,
  TAccountFeed extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  governanceAuthority: TransactionSigner<TAccountGovernanceAuthority>;
  oracleConfig?: Address<TAccountOracleConfig>;
  feed: Address<TAccountFeed>;
  systemProgram?: Address<TAccountSystemProgram>;
  tokenMint: ConfigureFeedInstructionDataArgs["tokenMint"];
  primary: ConfigureFeedInstructionDataArgs["primary"];
  backup: ConfigureFeedInstructionDataArgs["backup"];
  requiresStrict: ConfigureFeedInstructionDataArgs["requiresStrict"];
  maxPriceAge: ConfigureFeedInstructionDataArgs["maxPriceAge"];
  maxConfidenceBps: ConfigureFeedInstructionDataArgs["maxConfidenceBps"];
  maxDeviationBps: ConfigureFeedInstructionDataArgs["maxDeviationBps"];
};

export async function getConfigureFeedInstructionAsync<
  TAccountGovernanceAuthority extends string,
  TAccountOracleConfig extends string,
  TAccountFeed extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
>(
  input: ConfigureFeedAsyncInput<
    TAccountGovernanceAuthority,
    TAccountOracleConfig,
    TAccountFeed,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ConfigureFeedInstruction<
    TProgramAddress,
    TAccountGovernanceAuthority,
    TAccountOracleConfig,
    TAccountFeed,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPOSITE_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    governanceAuthority: {
      value: input.governanceAuthority ?? null,
      isWritable: true,
    },
    oracleConfig: { value: input.oracleConfig ?? null, isWritable: false },
    feed: { value: input.feed ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.oracleConfig.value) {
    accounts.oracleConfig.value = await findOracleConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("governanceAuthority", accounts.governanceAuthority),
      getAccountMeta("oracleConfig", accounts.oracleConfig),
      getAccountMeta("feed", accounts.feed),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getConfigureFeedInstructionDataEncoder().encode(
      args as ConfigureFeedInstructionDataArgs,
    ),
    programAddress,
  } as ConfigureFeedInstruction<
    TProgramAddress,
    TAccountGovernanceAuthority,
    TAccountOracleConfig,
    TAccountFeed,
    TAccountSystemProgram
  >);
}

export type ConfigureFeedInput<
  TAccountGovernanceAuthority extends string = string,
  TAccountOracleConfig extends string = string,
  TAccountFeed extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  governanceAuthority: TransactionSigner<TAccountGovernanceAuthority>;
  oracleConfig: Address<TAccountOracleConfig>;
  feed: Address<TAccountFeed>;
  systemProgram?: Address<TAccountSystemProgram>;
  tokenMint: ConfigureFeedInstructionDataArgs["tokenMint"];
  primary: ConfigureFeedInstructionDataArgs["primary"];
  backup: ConfigureFeedInstructionDataArgs["backup"];
  requiresStrict: ConfigureFeedInstructionDataArgs["requiresStrict"];
  maxPriceAge: ConfigureFeedInstructionDataArgs["maxPriceAge"];
  maxConfidenceBps: ConfigureFeedInstructionDataArgs["maxConfidenceBps"];
  maxDeviationBps: ConfigureFeedInstructionDataArgs["maxDeviationBps"];
};

export function getConfigureFeedInstruction<
  TAccountGovernanceAuthority extends string,
  TAccountOracleConfig extends string,
  TAccountFeed extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
>(
  input: ConfigureFeedInput<
    TAccountGovernanceAuthority,
    TAccountOracleConfig,
    TAccountFeed,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ConfigureFeedInstruction<
  TProgramAddress,
  TAccountGovernanceAuthority,
  TAccountOracleConfig,
  TAccountFeed,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPOSITE_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    governanceAuthority: {
      value: input.governanceAuthority ?? null,
      isWritable: true,
    },
    oracleConfig: { value: input.oracleConfig ?? null, isWritable: false },
    feed: { value: input.feed ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("governanceAuthority", accounts.governanceAuthority),
      getAccountMeta("oracleConfig", accounts.oracleConfig),
      getAccountMeta("feed", accounts.feed),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getConfigureFeedInstructionDataEncoder().encode(
      args as ConfigureFeedInstructionDataArgs,
    ),
    programAddress,
  } as ConfigureFeedInstruction<
    TProgramAddress,
    TAccountGovernanceAuthority,
    TAccountOracleConfig,
    TAccountFeed,
    TAccountSystemProgram
  >);
}

export type ParsedConfigureFeedInstruction<
  TProgram extends string = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    governanceAuthority: TAccountMetas[0];
    oracleConfig: TAccountMetas[1];
    feed: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ConfigureFeedInstructionData;
};

export function parseConfigureFeedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedConfigureFeedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      governanceAuthority: getNextAccount(),
      oracleConfig: getNextAccount(),
      feed: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getConfigureFeedInstructionDataDecoder().decode(instruction.data),
  };
}
