/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findFeedPda, findOracleConfigPda } from "../pdas";
import { COMPOSITE_ORACLE_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_CHALLENGE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([231, 253, 0, 151, 179, 94, 5, 152]);

export function getCancelChallengeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_CHALLENGE_DISCRIMINATOR,
  );
}

export type CancelChallengeInstruction<
  TProgram extends string = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
  TAccountChallenger extends string | AccountMeta<string> = string,
  TAccountOracleConfig extends string | AccountMeta<string> = string,
  TAccountFeed extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountChallenger extends string
        ? ReadonlySignerAccount<TAccountChallenger> &
            AccountSignerMeta<TAccountChallenger>
        : TAccountChallenger,
      TAccountOracleConfig extends string
        ? ReadonlyAccount<TAccountOracleConfig>
        : TAccountOracleConfig,
      TAccountFeed extends string
        ? WritableAccount<TAccountFeed>
        : TAccountFeed,
      ...TRemainingAccounts,
    ]
  >;

export type CancelChallengeInstructionData = {
  discriminator: ReadonlyUint8Array;
  tokenMint: Address;
};

export type CancelChallengeInstructionDataArgs = { tokenMint: Address };

export function getCancelChallengeInstructionDataEncoder(): FixedSizeEncoder<CancelChallengeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tokenMint", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CANCEL_CHALLENGE_DISCRIMINATOR }),
  );
}

export function getCancelChallengeInstructionDataDecoder(): FixedSizeDecoder<CancelChallengeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tokenMint", getAddressDecoder()],
  ]);
}

export function getCancelChallengeInstructionDataCodec(): FixedSizeCodec<
  CancelChallengeInstructionDataArgs,
  CancelChallengeInstructionData
> {
  return combineCodec(
    getCancelChallengeInstructionDataEncoder(),
    getCancelChallengeInstructionDataDecoder(),
  );
}

export type CancelChallengeAsyncInput<
  TAccountChallenger extends string = string,
  TAccountOracleConfig extends string = string,
  TAccountFeed extends string = string,
> = {
  /** Permissionless challenger. */
  challenger: TransactionSigner<TAccountChallenger>;
  oracleConfig?: Address<TAccountOracleConfig>;
  feed?: Address<TAccountFeed>;
  tokenMint: CancelChallengeInstructionDataArgs["tokenMint"];
};

export async function getCancelChallengeInstructionAsync<
  TAccountChallenger extends string,
  TAccountOracleConfig extends string,
  TAccountFeed extends string,
  TProgramAddress extends Address = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
>(
  input: CancelChallengeAsyncInput<
    TAccountChallenger,
    TAccountOracleConfig,
    TAccountFeed
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelChallengeInstruction<
    TProgramAddress,
    TAccountChallenger,
    TAccountOracleConfig,
    TAccountFeed
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPOSITE_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    challenger: { value: input.challenger ?? null, isWritable: false },
    oracleConfig: { value: input.oracleConfig ?? null, isWritable: false },
    feed: { value: input.feed ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.oracleConfig.value) {
    accounts.oracleConfig.value = await findOracleConfigPda();
  }
  if (!accounts.feed.value) {
    accounts.feed.value = await findFeedPda({
      tokenMint: getNonNullResolvedInstructionInput(
        "tokenMint",
        args.tokenMint,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("challenger", accounts.challenger),
      getAccountMeta("oracleConfig", accounts.oracleConfig),
      getAccountMeta("feed", accounts.feed),
    ],
    data: getCancelChallengeInstructionDataEncoder().encode(
      args as CancelChallengeInstructionDataArgs,
    ),
    programAddress,
  } as CancelChallengeInstruction<
    TProgramAddress,
    TAccountChallenger,
    TAccountOracleConfig,
    TAccountFeed
  >);
}

export type CancelChallengeInput<
  TAccountChallenger extends string = string,
  TAccountOracleConfig extends string = string,
  TAccountFeed extends string = string,
> = {
  /** Permissionless challenger. */
  challenger: TransactionSigner<TAccountChallenger>;
  oracleConfig: Address<TAccountOracleConfig>;
  feed: Address<TAccountFeed>;
  tokenMint: CancelChallengeInstructionDataArgs["tokenMint"];
};

export function getCancelChallengeInstruction<
  TAccountChallenger extends string,
  TAccountOracleConfig extends string,
  TAccountFeed extends string,
  TProgramAddress extends Address = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
>(
  input: CancelChallengeInput<
    TAccountChallenger,
    TAccountOracleConfig,
    TAccountFeed
  >,
  config?: { programAddress?: TProgramAddress },
): CancelChallengeInstruction<
  TProgramAddress,
  TAccountChallenger,
  TAccountOracleConfig,
  TAccountFeed
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPOSITE_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    challenger: { value: input.challenger ?? null, isWritable: false },
    oracleConfig: { value: input.oracleConfig ?? null, isWritable: false },
    feed: { value: input.feed ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("challenger", accounts.challenger),
      getAccountMeta("oracleConfig", accounts.oracleConfig),
      getAccountMeta("feed", accounts.feed),
    ],
    data: getCancelChallengeInstructionDataEncoder().encode(
      args as CancelChallengeInstructionDataArgs,
    ),
    programAddress,
  } as CancelChallengeInstruction<
    TProgramAddress,
    TAccountChallenger,
    TAccountOracleConfig,
    TAccountFeed
  >);
}

export type ParsedCancelChallengeInstruction<
  TProgram extends string = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Permissionless challenger. */
    challenger: TAccountMetas[0];
    oracleConfig: TAccountMetas[1];
    feed: TAccountMetas[2];
  };
  data: CancelChallengeInstructionData;
};

export function parseCancelChallengeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelChallengeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      challenger: getNextAccount(),
      oracleConfig: getNextAccount(),
      feed: getNextAccount(),
    },
    data: getCancelChallengeInstructionDataDecoder().decode(instruction.data),
  };
}
