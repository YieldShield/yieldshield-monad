/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findFeedPda } from "../pdas";
import { COMPOSITE_ORACLE_PROGRAM_ADDRESS } from "../programs";

export const GET_PRICE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  238, 38, 193, 106, 228, 32, 210, 33,
]);

export function getGetPriceDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GET_PRICE_DISCRIMINATOR);
}

export type GetPriceInstruction<
  TProgram extends string = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
  TAccountFeed extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFeed extends string
        ? ReadonlyAccount<TAccountFeed>
        : TAccountFeed,
      ...TRemainingAccounts,
    ]
  >;

export type GetPriceInstructionData = {
  discriminator: ReadonlyUint8Array;
  tokenMint: Address;
};

export type GetPriceInstructionDataArgs = { tokenMint: Address };

export function getGetPriceInstructionDataEncoder(): FixedSizeEncoder<GetPriceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tokenMint", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: GET_PRICE_DISCRIMINATOR }),
  );
}

export function getGetPriceInstructionDataDecoder(): FixedSizeDecoder<GetPriceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tokenMint", getAddressDecoder()],
  ]);
}

export function getGetPriceInstructionDataCodec(): FixedSizeCodec<
  GetPriceInstructionDataArgs,
  GetPriceInstructionData
> {
  return combineCodec(
    getGetPriceInstructionDataEncoder(),
    getGetPriceInstructionDataDecoder(),
  );
}

export type GetPriceAsyncInput<TAccountFeed extends string = string> = {
  feed?: Address<TAccountFeed>;
  tokenMint: GetPriceInstructionDataArgs["tokenMint"];
};

export async function getGetPriceInstructionAsync<
  TAccountFeed extends string,
  TProgramAddress extends Address = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
>(
  input: GetPriceAsyncInput<TAccountFeed>,
  config?: { programAddress?: TProgramAddress },
): Promise<GetPriceInstruction<TProgramAddress, TAccountFeed>> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPOSITE_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    feed: { value: input.feed ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.feed.value) {
    accounts.feed.value = await findFeedPda({
      tokenMint: getNonNullResolvedInstructionInput(
        "tokenMint",
        args.tokenMint,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("feed", accounts.feed)],
    data: getGetPriceInstructionDataEncoder().encode(
      args as GetPriceInstructionDataArgs,
    ),
    programAddress,
  } as GetPriceInstruction<TProgramAddress, TAccountFeed>);
}

export type GetPriceInput<TAccountFeed extends string = string> = {
  feed: Address<TAccountFeed>;
  tokenMint: GetPriceInstructionDataArgs["tokenMint"];
};

export function getGetPriceInstruction<
  TAccountFeed extends string,
  TProgramAddress extends Address = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
>(
  input: GetPriceInput<TAccountFeed>,
  config?: { programAddress?: TProgramAddress },
): GetPriceInstruction<TProgramAddress, TAccountFeed> {
  // Program address.
  const programAddress =
    config?.programAddress ?? COMPOSITE_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    feed: { value: input.feed ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("feed", accounts.feed)],
    data: getGetPriceInstructionDataEncoder().encode(
      args as GetPriceInstructionDataArgs,
    ),
    programAddress,
  } as GetPriceInstruction<TProgramAddress, TAccountFeed>);
}

export type ParsedGetPriceInstruction<
  TProgram extends string = typeof COMPOSITE_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    feed: TAccountMetas[0];
  };
  data: GetPriceInstructionData;
};

export function parseGetPriceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGetPriceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { feed: getNextAccount() },
    data: getGetPriceInstructionDataDecoder().decode(instruction.data),
  };
}
