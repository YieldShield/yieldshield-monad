/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getFeedSlotDecoder,
  getFeedSlotEncoder,
  type FeedSlot,
  type FeedSlotArgs,
} from "../types";

export const FEED_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  75, 97, 12, 15, 89, 221, 78, 71,
]);

export function getFeedConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(FEED_CONFIG_DISCRIMINATOR);
}

export type FeedConfig = {
  discriminator: ReadonlyUint8Array;
  /** The token this feed prices. */
  tokenMint: Address;
  /** Primary feed slot. */
  primary: FeedSlot;
  /** Optional backup feed slot (dual-feed); `None` = single-feed token. */
  backup: Option<FeedSlot>;
  /** True once a finalized challenge has switched routing to the backup (AC-003). */
  isBackupActive: boolean;
  /** Active-challenge start time; `0` when no challenge is in flight. */
  challengeStart: bigint;
  /** Address that opened the active challenge; `None` when idle. */
  challenger: Option<Address>;
  /** Last challenge timestamp (enforces `COOLDOWN_PERIOD`, CON-002). */
  lastChallengeTime: bigint;
  /** True if this token must use the strict protected-price path (SEC-006). */
  requiresStrict: boolean;
  /** Per-token staleness window (seconds), bounded by `MAX_PRICE_AGE_LIMIT`. */
  maxPriceAge: bigint;
  /** Per-token max confidence width (bps). */
  maxConfidenceBps: bigint;
  /** Per-token deviation circuit-breaker threshold (bps). */
  maxDeviationBps: bigint;
  /** Cached primary price (USD 8-decimal) for `Manual` slots; `0` until first push. */
  primaryPrice: bigint;
  /** Publish time of `primary_price` (unix seconds) — drives staleness. */
  primaryPublishTime: bigint;
  /** Cached backup price (USD 8-decimal) for `Manual` slots. */
  backupPrice: bigint;
  /** Publish time of `backup_price`. */
  backupPublishTime: bigint;
  /** PDA bump. */
  bump: number;
};

export type FeedConfigArgs = {
  /** The token this feed prices. */
  tokenMint: Address;
  /** Primary feed slot. */
  primary: FeedSlotArgs;
  /** Optional backup feed slot (dual-feed); `None` = single-feed token. */
  backup: OptionOrNullable<FeedSlotArgs>;
  /** True once a finalized challenge has switched routing to the backup (AC-003). */
  isBackupActive: boolean;
  /** Active-challenge start time; `0` when no challenge is in flight. */
  challengeStart: number | bigint;
  /** Address that opened the active challenge; `None` when idle. */
  challenger: OptionOrNullable<Address>;
  /** Last challenge timestamp (enforces `COOLDOWN_PERIOD`, CON-002). */
  lastChallengeTime: number | bigint;
  /** True if this token must use the strict protected-price path (SEC-006). */
  requiresStrict: boolean;
  /** Per-token staleness window (seconds), bounded by `MAX_PRICE_AGE_LIMIT`. */
  maxPriceAge: number | bigint;
  /** Per-token max confidence width (bps). */
  maxConfidenceBps: number | bigint;
  /** Per-token deviation circuit-breaker threshold (bps). */
  maxDeviationBps: number | bigint;
  /** Cached primary price (USD 8-decimal) for `Manual` slots; `0` until first push. */
  primaryPrice: number | bigint;
  /** Publish time of `primary_price` (unix seconds) — drives staleness. */
  primaryPublishTime: number | bigint;
  /** Cached backup price (USD 8-decimal) for `Manual` slots. */
  backupPrice: number | bigint;
  /** Publish time of `backup_price`. */
  backupPublishTime: number | bigint;
  /** PDA bump. */
  bump: number;
};

/** Gets the encoder for {@link FeedConfigArgs} account data. */
export function getFeedConfigEncoder(): Encoder<FeedConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tokenMint", getAddressEncoder()],
      ["primary", getFeedSlotEncoder()],
      ["backup", getOptionEncoder(getFeedSlotEncoder())],
      ["isBackupActive", getBooleanEncoder()],
      ["challengeStart", getI64Encoder()],
      ["challenger", getOptionEncoder(getAddressEncoder())],
      ["lastChallengeTime", getI64Encoder()],
      ["requiresStrict", getBooleanEncoder()],
      ["maxPriceAge", getU64Encoder()],
      ["maxConfidenceBps", getU64Encoder()],
      ["maxDeviationBps", getU64Encoder()],
      ["primaryPrice", getU128Encoder()],
      ["primaryPublishTime", getI64Encoder()],
      ["backupPrice", getU128Encoder()],
      ["backupPublishTime", getI64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: FEED_CONFIG_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link FeedConfig} account data. */
export function getFeedConfigDecoder(): Decoder<FeedConfig> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tokenMint", getAddressDecoder()],
    ["primary", getFeedSlotDecoder()],
    ["backup", getOptionDecoder(getFeedSlotDecoder())],
    ["isBackupActive", getBooleanDecoder()],
    ["challengeStart", getI64Decoder()],
    ["challenger", getOptionDecoder(getAddressDecoder())],
    ["lastChallengeTime", getI64Decoder()],
    ["requiresStrict", getBooleanDecoder()],
    ["maxPriceAge", getU64Decoder()],
    ["maxConfidenceBps", getU64Decoder()],
    ["maxDeviationBps", getU64Decoder()],
    ["primaryPrice", getU128Decoder()],
    ["primaryPublishTime", getI64Decoder()],
    ["backupPrice", getU128Decoder()],
    ["backupPublishTime", getI64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link FeedConfig} account data. */
export function getFeedConfigCodec(): Codec<FeedConfigArgs, FeedConfig> {
  return combineCodec(getFeedConfigEncoder(), getFeedConfigDecoder());
}

export function decodeFeedConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<FeedConfig, TAddress>;
export function decodeFeedConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<FeedConfig, TAddress>;
export function decodeFeedConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<FeedConfig, TAddress> | MaybeAccount<FeedConfig, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getFeedConfigDecoder(),
  );
}

export async function fetchFeedConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<FeedConfig, TAddress>> {
  const maybeAccount = await fetchMaybeFeedConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeFeedConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<FeedConfig, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeFeedConfig(maybeAccount);
}

export async function fetchAllFeedConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<FeedConfig>[]> {
  const maybeAccounts = await fetchAllMaybeFeedConfig(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeFeedConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<FeedConfig>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeFeedConfig(maybeAccount));
}
