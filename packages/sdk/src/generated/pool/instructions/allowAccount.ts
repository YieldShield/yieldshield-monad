/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAllowEntryPda } from "../pdas";
import { SPLIT_RISK_POOL_PROGRAM_ADDRESS } from "../programs";

export const ALLOW_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  81, 64, 221, 184, 236, 221, 185, 197,
]);

export function getAllowAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ALLOW_ACCOUNT_DISCRIMINATOR,
  );
}

export type AllowAccountInstruction<
  TProgram extends string = typeof SPLIT_RISK_POOL_PROGRAM_ADDRESS,
  TAccountAclAuthority extends string | AccountMeta<string> = string,
  TAccountAllowEntry extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAclAuthority extends string
        ? WritableSignerAccount<TAccountAclAuthority> &
            AccountSignerMeta<TAccountAclAuthority>
        : TAccountAclAuthority,
      TAccountAllowEntry extends string
        ? WritableAccount<TAccountAllowEntry>
        : TAccountAllowEntry,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AllowAccountInstructionData = {
  discriminator: ReadonlyUint8Array;
  account: Address;
};

export type AllowAccountInstructionDataArgs = { account: Address };

export function getAllowAccountInstructionDataEncoder(): FixedSizeEncoder<AllowAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["account", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: ALLOW_ACCOUNT_DISCRIMINATOR }),
  );
}

export function getAllowAccountInstructionDataDecoder(): FixedSizeDecoder<AllowAccountInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["account", getAddressDecoder()],
  ]);
}

export function getAllowAccountInstructionDataCodec(): FixedSizeCodec<
  AllowAccountInstructionDataArgs,
  AllowAccountInstructionData
> {
  return combineCodec(
    getAllowAccountInstructionDataEncoder(),
    getAllowAccountInstructionDataDecoder(),
  );
}

export type AllowAccountAsyncInput<
  TAccountAclAuthority extends string = string,
  TAccountAllowEntry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The access-control authority (owns this allowlist). */
  aclAuthority: TransactionSigner<TAccountAclAuthority>;
  allowEntry?: Address<TAccountAllowEntry>;
  systemProgram?: Address<TAccountSystemProgram>;
  account: AllowAccountInstructionDataArgs["account"];
};

export async function getAllowAccountInstructionAsync<
  TAccountAclAuthority extends string,
  TAccountAllowEntry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SPLIT_RISK_POOL_PROGRAM_ADDRESS,
>(
  input: AllowAccountAsyncInput<
    TAccountAclAuthority,
    TAccountAllowEntry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AllowAccountInstruction<
    TProgramAddress,
    TAccountAclAuthority,
    TAccountAllowEntry,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPLIT_RISK_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    aclAuthority: { value: input.aclAuthority ?? null, isWritable: true },
    allowEntry: { value: input.allowEntry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.allowEntry.value) {
    accounts.allowEntry.value = await findAllowEntryPda({
      aclAuthority: getAddressFromResolvedInstructionAccount(
        "aclAuthority",
        accounts.aclAuthority.value,
      ),
      account: getNonNullResolvedInstructionInput("account", args.account),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("aclAuthority", accounts.aclAuthority),
      getAccountMeta("allowEntry", accounts.allowEntry),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAllowAccountInstructionDataEncoder().encode(
      args as AllowAccountInstructionDataArgs,
    ),
    programAddress,
  } as AllowAccountInstruction<
    TProgramAddress,
    TAccountAclAuthority,
    TAccountAllowEntry,
    TAccountSystemProgram
  >);
}

export type AllowAccountInput<
  TAccountAclAuthority extends string = string,
  TAccountAllowEntry extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The access-control authority (owns this allowlist). */
  aclAuthority: TransactionSigner<TAccountAclAuthority>;
  allowEntry: Address<TAccountAllowEntry>;
  systemProgram?: Address<TAccountSystemProgram>;
  account: AllowAccountInstructionDataArgs["account"];
};

export function getAllowAccountInstruction<
  TAccountAclAuthority extends string,
  TAccountAllowEntry extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SPLIT_RISK_POOL_PROGRAM_ADDRESS,
>(
  input: AllowAccountInput<
    TAccountAclAuthority,
    TAccountAllowEntry,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AllowAccountInstruction<
  TProgramAddress,
  TAccountAclAuthority,
  TAccountAllowEntry,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPLIT_RISK_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    aclAuthority: { value: input.aclAuthority ?? null, isWritable: true },
    allowEntry: { value: input.allowEntry ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("aclAuthority", accounts.aclAuthority),
      getAccountMeta("allowEntry", accounts.allowEntry),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAllowAccountInstructionDataEncoder().encode(
      args as AllowAccountInstructionDataArgs,
    ),
    programAddress,
  } as AllowAccountInstruction<
    TProgramAddress,
    TAccountAclAuthority,
    TAccountAllowEntry,
    TAccountSystemProgram
  >);
}

export type ParsedAllowAccountInstruction<
  TProgram extends string = typeof SPLIT_RISK_POOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The access-control authority (owns this allowlist). */
    aclAuthority: TAccountMetas[0];
    allowEntry: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: AllowAccountInstructionData;
};

export function parseAllowAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAllowAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      aclAuthority: getNextAccount(),
      allowEntry: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAllowAccountInstructionDataDecoder().decode(instruction.data),
  };
}
