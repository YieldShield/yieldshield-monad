/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findFactoryPda } from "../pdas";
import { SPLIT_RISK_POOL_PROGRAM_ADDRESS } from "../programs";

export const INIT_FACTORY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  65, 136, 219, 177, 234, 197, 24, 39,
]);

export function getInitFactoryDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_FACTORY_DISCRIMINATOR,
  );
}

export type InitFactoryInstruction<
  TProgram extends string = typeof SPLIT_RISK_POOL_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountFactory extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountFactory extends string
        ? WritableAccount<TAccountFactory>
        : TAccountFactory,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitFactoryInstructionData = {
  discriminator: ReadonlyUint8Array;
  governanceAuthority: Address;
  defaultOracle: Address;
  minimumCreationBondUsd: bigint;
  maxActivePools: number;
};

export type InitFactoryInstructionDataArgs = {
  governanceAuthority: Address;
  defaultOracle: Address;
  minimumCreationBondUsd: number | bigint;
  maxActivePools: number;
};

export function getInitFactoryInstructionDataEncoder(): FixedSizeEncoder<InitFactoryInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["governanceAuthority", getAddressEncoder()],
      ["defaultOracle", getAddressEncoder()],
      ["minimumCreationBondUsd", getU128Encoder()],
      ["maxActivePools", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_FACTORY_DISCRIMINATOR }),
  );
}

export function getInitFactoryInstructionDataDecoder(): FixedSizeDecoder<InitFactoryInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["governanceAuthority", getAddressDecoder()],
    ["defaultOracle", getAddressDecoder()],
    ["minimumCreationBondUsd", getU128Decoder()],
    ["maxActivePools", getU32Decoder()],
  ]);
}

export function getInitFactoryInstructionDataCodec(): FixedSizeCodec<
  InitFactoryInstructionDataArgs,
  InitFactoryInstructionData
> {
  return combineCodec(
    getInitFactoryInstructionDataEncoder(),
    getInitFactoryInstructionDataDecoder(),
  );
}

export type InitFactoryAsyncInput<
  TAccountPayer extends string = string,
  TAccountFactory extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays rent for the `Factory` PDA. */
  payer: TransactionSigner<TAccountPayer>;
  /** The registry singleton — seeds `["factory"]`. */
  factory?: Address<TAccountFactory>;
  systemProgram?: Address<TAccountSystemProgram>;
  governanceAuthority: InitFactoryInstructionDataArgs["governanceAuthority"];
  defaultOracle: InitFactoryInstructionDataArgs["defaultOracle"];
  minimumCreationBondUsd: InitFactoryInstructionDataArgs["minimumCreationBondUsd"];
  maxActivePools: InitFactoryInstructionDataArgs["maxActivePools"];
};

export async function getInitFactoryInstructionAsync<
  TAccountPayer extends string,
  TAccountFactory extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SPLIT_RISK_POOL_PROGRAM_ADDRESS,
>(
  input: InitFactoryAsyncInput<
    TAccountPayer,
    TAccountFactory,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitFactoryInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountFactory,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPLIT_RISK_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    factory: { value: input.factory ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.factory.value) {
    accounts.factory.value = await findFactoryPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("factory", accounts.factory),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitFactoryInstructionDataEncoder().encode(
      args as InitFactoryInstructionDataArgs,
    ),
    programAddress,
  } as InitFactoryInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountFactory,
    TAccountSystemProgram
  >);
}

export type InitFactoryInput<
  TAccountPayer extends string = string,
  TAccountFactory extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays rent for the `Factory` PDA. */
  payer: TransactionSigner<TAccountPayer>;
  /** The registry singleton — seeds `["factory"]`. */
  factory: Address<TAccountFactory>;
  systemProgram?: Address<TAccountSystemProgram>;
  governanceAuthority: InitFactoryInstructionDataArgs["governanceAuthority"];
  defaultOracle: InitFactoryInstructionDataArgs["defaultOracle"];
  minimumCreationBondUsd: InitFactoryInstructionDataArgs["minimumCreationBondUsd"];
  maxActivePools: InitFactoryInstructionDataArgs["maxActivePools"];
};

export function getInitFactoryInstruction<
  TAccountPayer extends string,
  TAccountFactory extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SPLIT_RISK_POOL_PROGRAM_ADDRESS,
>(
  input: InitFactoryInput<
    TAccountPayer,
    TAccountFactory,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitFactoryInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountFactory,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SPLIT_RISK_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    factory: { value: input.factory ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("factory", accounts.factory),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitFactoryInstructionDataEncoder().encode(
      args as InitFactoryInstructionDataArgs,
    ),
    programAddress,
  } as InitFactoryInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountFactory,
    TAccountSystemProgram
  >);
}

export type ParsedInitFactoryInstruction<
  TProgram extends string = typeof SPLIT_RISK_POOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays rent for the `Factory` PDA. */
    payer: TAccountMetas[0];
    /** The registry singleton — seeds `["factory"]`. */
    factory: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitFactoryInstructionData;
};

export function parseInitFactoryInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitFactoryInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      factory: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitFactoryInstructionDataDecoder().decode(instruction.data),
  };
}
