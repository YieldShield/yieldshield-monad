/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const POOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  241, 154, 109, 4, 17, 177, 109, 188,
]);

export function getPoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(POOL_DISCRIMINATOR);
}

export type Pool = {
  discriminator: ReadonlyUint8Array;
  /** Shielded (protected) asset mint. */
  shieldedMint: Address;
  /** Backing (underwriter) asset mint. */
  backingMint: Address;
  /** Pool creator (bond payer / pool-fee recipient default). */
  creator: Address;
  /** `COMMISSION_RATE` in bps (CON-101). */
  commissionRateBp: bigint;
  /** `POOL_FEE` in bps (CON-102). */
  poolFeeBp: bigint;
  /** `COLLATERAL_RATIO` in bps (CON-104). */
  collateralRatioBp: bigint;
  /** Σ extant shield position amounts (native shielded-token units). */
  totalShieldedTokens: bigint;
  /** Σ extant protector claims (native backing-token units). */
  totalProtectorTokens: bigint;
  /** Σ extant shield position USD values (8-decimal). */
  totalValueAtDeposit: bigint;
  /** Σ extant shield `collateral_amount` (backing-token units). */
  totalShieldCollateralAmount: bigint;
  /** Commission owed to protectors (claimable, not user-withdrawable). */
  accumulatedCommissions: bigint;
  /** Pool-creator fee owed. */
  accumulatedPoolFee: bigint;
  /** Protocol fee owed. */
  accumulatedProtocolFee: bigint;
  /** MasterChef `reward_per_share`, scaled by `REWARD_PRECISION`, stored as U256 LE bytes. */
  rewardPerShare: ReadonlyUint8Array;
  /** Total normalized protector shares in the current epoch. */
  totalProtectorShares: bigint;
  /** Current protector epoch (increments on full backing drain via activation). */
  protectorEpoch: bigint;
  /**
   * Reward tokens that did not yet move the per-share accumulator (carried forward; EVM
   * `pendingProtectorRewardDust`).
   */
  pendingProtectorRewardDust: bigint;
  /**
   * Commission reserved within the current epoch (snapshotted on epoch advance; EVM
   * `currentEpochCommissionReserve`).
   */
  currentEpochCommissionReserve: bigint;
  /**
   * Commission reserved across all expired epochs awaiting settlement (EVM
   * `historicalCommissionReserve`).
   */
  historicalCommissionReserve: bigint;
  /** Lifetime commission ever accumulated (monotonic; EVM `totalCommissionsEverAccumulated`). */
  totalCommissionsEverAccumulated: bigint;
  /**
   * `10^shielded_decimals` — the scale used to convert shielded amounts to/from USD value
   * (EVM immutable `shieldedTokenScale`).
   */
  shieldedTokenScale: bigint;
  /**
   * Creation bond (backing-token units) held in the `["bond", pool]` escrow; refunded to the
   * creator at `close_pool` (CON-109).
   */
  creationBondAmount: bigint;
  /** SEC-108: set when the shielded token exhibits a transfer shortfall. */
  shieldedTokenTransferIntegrityBroken: boolean;
  /** Whether the pool is active (false after deactivation). */
  active: boolean;
  /** Monotonic position-index source for `["pos_mint", pool, index]` (OI-4; never decremented). */
  positionIndex: bigint;
  /** Count of extant shield positions. */
  shieldPositionCount: bigint;
  /** Count of extant protector positions. */
  protectorPositionCount: bigint;
  /** PDA bump. */
  bump: number;
};

export type PoolArgs = {
  /** Shielded (protected) asset mint. */
  shieldedMint: Address;
  /** Backing (underwriter) asset mint. */
  backingMint: Address;
  /** Pool creator (bond payer / pool-fee recipient default). */
  creator: Address;
  /** `COMMISSION_RATE` in bps (CON-101). */
  commissionRateBp: number | bigint;
  /** `POOL_FEE` in bps (CON-102). */
  poolFeeBp: number | bigint;
  /** `COLLATERAL_RATIO` in bps (CON-104). */
  collateralRatioBp: number | bigint;
  /** Σ extant shield position amounts (native shielded-token units). */
  totalShieldedTokens: number | bigint;
  /** Σ extant protector claims (native backing-token units). */
  totalProtectorTokens: number | bigint;
  /** Σ extant shield position USD values (8-decimal). */
  totalValueAtDeposit: number | bigint;
  /** Σ extant shield `collateral_amount` (backing-token units). */
  totalShieldCollateralAmount: number | bigint;
  /** Commission owed to protectors (claimable, not user-withdrawable). */
  accumulatedCommissions: number | bigint;
  /** Pool-creator fee owed. */
  accumulatedPoolFee: number | bigint;
  /** Protocol fee owed. */
  accumulatedProtocolFee: number | bigint;
  /** MasterChef `reward_per_share`, scaled by `REWARD_PRECISION`, stored as U256 LE bytes. */
  rewardPerShare: ReadonlyUint8Array;
  /** Total normalized protector shares in the current epoch. */
  totalProtectorShares: number | bigint;
  /** Current protector epoch (increments on full backing drain via activation). */
  protectorEpoch: number | bigint;
  /**
   * Reward tokens that did not yet move the per-share accumulator (carried forward; EVM
   * `pendingProtectorRewardDust`).
   */
  pendingProtectorRewardDust: number | bigint;
  /**
   * Commission reserved within the current epoch (snapshotted on epoch advance; EVM
   * `currentEpochCommissionReserve`).
   */
  currentEpochCommissionReserve: number | bigint;
  /**
   * Commission reserved across all expired epochs awaiting settlement (EVM
   * `historicalCommissionReserve`).
   */
  historicalCommissionReserve: number | bigint;
  /** Lifetime commission ever accumulated (monotonic; EVM `totalCommissionsEverAccumulated`). */
  totalCommissionsEverAccumulated: number | bigint;
  /**
   * `10^shielded_decimals` — the scale used to convert shielded amounts to/from USD value
   * (EVM immutable `shieldedTokenScale`).
   */
  shieldedTokenScale: number | bigint;
  /**
   * Creation bond (backing-token units) held in the `["bond", pool]` escrow; refunded to the
   * creator at `close_pool` (CON-109).
   */
  creationBondAmount: number | bigint;
  /** SEC-108: set when the shielded token exhibits a transfer shortfall. */
  shieldedTokenTransferIntegrityBroken: boolean;
  /** Whether the pool is active (false after deactivation). */
  active: boolean;
  /** Monotonic position-index source for `["pos_mint", pool, index]` (OI-4; never decremented). */
  positionIndex: number | bigint;
  /** Count of extant shield positions. */
  shieldPositionCount: number | bigint;
  /** Count of extant protector positions. */
  protectorPositionCount: number | bigint;
  /** PDA bump. */
  bump: number;
};

/** Gets the encoder for {@link PoolArgs} account data. */
export function getPoolEncoder(): FixedSizeEncoder<PoolArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["shieldedMint", getAddressEncoder()],
      ["backingMint", getAddressEncoder()],
      ["creator", getAddressEncoder()],
      ["commissionRateBp", getU64Encoder()],
      ["poolFeeBp", getU64Encoder()],
      ["collateralRatioBp", getU64Encoder()],
      ["totalShieldedTokens", getU128Encoder()],
      ["totalProtectorTokens", getU128Encoder()],
      ["totalValueAtDeposit", getU128Encoder()],
      ["totalShieldCollateralAmount", getU128Encoder()],
      ["accumulatedCommissions", getU128Encoder()],
      ["accumulatedPoolFee", getU128Encoder()],
      ["accumulatedProtocolFee", getU128Encoder()],
      ["rewardPerShare", fixEncoderSize(getBytesEncoder(), 32)],
      ["totalProtectorShares", getU128Encoder()],
      ["protectorEpoch", getU64Encoder()],
      ["pendingProtectorRewardDust", getU128Encoder()],
      ["currentEpochCommissionReserve", getU128Encoder()],
      ["historicalCommissionReserve", getU128Encoder()],
      ["totalCommissionsEverAccumulated", getU128Encoder()],
      ["shieldedTokenScale", getU128Encoder()],
      ["creationBondAmount", getU64Encoder()],
      ["shieldedTokenTransferIntegrityBroken", getBooleanEncoder()],
      ["active", getBooleanEncoder()],
      ["positionIndex", getU64Encoder()],
      ["shieldPositionCount", getU64Encoder()],
      ["protectorPositionCount", getU64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: POOL_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Pool} account data. */
export function getPoolDecoder(): FixedSizeDecoder<Pool> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["shieldedMint", getAddressDecoder()],
    ["backingMint", getAddressDecoder()],
    ["creator", getAddressDecoder()],
    ["commissionRateBp", getU64Decoder()],
    ["poolFeeBp", getU64Decoder()],
    ["collateralRatioBp", getU64Decoder()],
    ["totalShieldedTokens", getU128Decoder()],
    ["totalProtectorTokens", getU128Decoder()],
    ["totalValueAtDeposit", getU128Decoder()],
    ["totalShieldCollateralAmount", getU128Decoder()],
    ["accumulatedCommissions", getU128Decoder()],
    ["accumulatedPoolFee", getU128Decoder()],
    ["accumulatedProtocolFee", getU128Decoder()],
    ["rewardPerShare", fixDecoderSize(getBytesDecoder(), 32)],
    ["totalProtectorShares", getU128Decoder()],
    ["protectorEpoch", getU64Decoder()],
    ["pendingProtectorRewardDust", getU128Decoder()],
    ["currentEpochCommissionReserve", getU128Decoder()],
    ["historicalCommissionReserve", getU128Decoder()],
    ["totalCommissionsEverAccumulated", getU128Decoder()],
    ["shieldedTokenScale", getU128Decoder()],
    ["creationBondAmount", getU64Decoder()],
    ["shieldedTokenTransferIntegrityBroken", getBooleanDecoder()],
    ["active", getBooleanDecoder()],
    ["positionIndex", getU64Decoder()],
    ["shieldPositionCount", getU64Decoder()],
    ["protectorPositionCount", getU64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Pool} account data. */
export function getPoolCodec(): FixedSizeCodec<PoolArgs, Pool> {
  return combineCodec(getPoolEncoder(), getPoolDecoder());
}

export function decodePool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Pool, TAddress>;
export function decodePool<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Pool, TAddress>;
export function decodePool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Pool, TAddress> | MaybeAccount<Pool, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPoolDecoder(),
  );
}

export async function fetchPool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Pool, TAddress>> {
  const maybeAccount = await fetchMaybePool(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Pool, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePool(maybeAccount);
}

export async function fetchAllPool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Pool>[]> {
  const maybeAccounts = await fetchAllMaybePool(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Pool>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePool(maybeAccount));
}

export function getPoolSize(): number {
  return 411;
}
