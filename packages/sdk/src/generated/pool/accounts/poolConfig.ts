/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const POOL_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  26, 108, 14, 123, 116, 230, 129, 43,
]);

export function getPoolConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(POOL_CONFIG_DISCRIMINATOR);
}

export type PoolConfig = {
  discriminator: ReadonlyUint8Array;
  /** Minimum shielded deposit (native units). */
  shieldedMinDeposit: bigint;
  /** Maximum shielded deposit (native units). */
  shieldedMaxDeposit: bigint;
  /** Minimum backing deposit (native units). */
  backingMinDeposit: bigint;
  /** Maximum backing deposit (native units). */
  backingMaxDeposit: bigint;
  /** USD TVL cap (8-decimal; REQ-008). */
  maxTvlUsd: bigint;
  /** Minimum pool time before cross-asset activation (CON-106; REQ-004). */
  minimumPoolTime: bigint;
  /** Protector unlock cooldown (CON-105; REQ-005). */
  unlockDuration: bigint;
  /** Protocol fee in bps (CON-103). */
  protocolFeeBp: bigint;
  /** Recipient of the protocol fee. */
  protocolFeeRecipient: Address;
  /** Recipient of the pool-creator fee. */
  poolFeeRecipient: Address;
  /** Oracle config/program used for this pool's price reads (overrides `Factory::default_oracle`). */
  priceOracle: Address;
  /** Optional access-control program / allowlist PDA (WS7; `None` = open). */
  accessControl: Option<Address>;
  /** Shield NFT transfer-lock duration (CON-108). */
  shieldTransferLock: bigint;
  /** Protector NFT transfer-lock duration (CON-108/POS-004). */
  protectorTransferLock: bigint;
  /** PDA bump. */
  bump: number;
};

export type PoolConfigArgs = {
  /** Minimum shielded deposit (native units). */
  shieldedMinDeposit: number | bigint;
  /** Maximum shielded deposit (native units). */
  shieldedMaxDeposit: number | bigint;
  /** Minimum backing deposit (native units). */
  backingMinDeposit: number | bigint;
  /** Maximum backing deposit (native units). */
  backingMaxDeposit: number | bigint;
  /** USD TVL cap (8-decimal; REQ-008). */
  maxTvlUsd: number | bigint;
  /** Minimum pool time before cross-asset activation (CON-106; REQ-004). */
  minimumPoolTime: number | bigint;
  /** Protector unlock cooldown (CON-105; REQ-005). */
  unlockDuration: number | bigint;
  /** Protocol fee in bps (CON-103). */
  protocolFeeBp: number | bigint;
  /** Recipient of the protocol fee. */
  protocolFeeRecipient: Address;
  /** Recipient of the pool-creator fee. */
  poolFeeRecipient: Address;
  /** Oracle config/program used for this pool's price reads (overrides `Factory::default_oracle`). */
  priceOracle: Address;
  /** Optional access-control program / allowlist PDA (WS7; `None` = open). */
  accessControl: OptionOrNullable<Address>;
  /** Shield NFT transfer-lock duration (CON-108). */
  shieldTransferLock: number | bigint;
  /** Protector NFT transfer-lock duration (CON-108/POS-004). */
  protectorTransferLock: number | bigint;
  /** PDA bump. */
  bump: number;
};

/** Gets the encoder for {@link PoolConfigArgs} account data. */
export function getPoolConfigEncoder(): Encoder<PoolConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["shieldedMinDeposit", getU64Encoder()],
      ["shieldedMaxDeposit", getU64Encoder()],
      ["backingMinDeposit", getU64Encoder()],
      ["backingMaxDeposit", getU64Encoder()],
      ["maxTvlUsd", getU128Encoder()],
      ["minimumPoolTime", getU64Encoder()],
      ["unlockDuration", getU64Encoder()],
      ["protocolFeeBp", getU64Encoder()],
      ["protocolFeeRecipient", getAddressEncoder()],
      ["poolFeeRecipient", getAddressEncoder()],
      ["priceOracle", getAddressEncoder()],
      ["accessControl", getOptionEncoder(getAddressEncoder())],
      ["shieldTransferLock", getU64Encoder()],
      ["protectorTransferLock", getU64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: POOL_CONFIG_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link PoolConfig} account data. */
export function getPoolConfigDecoder(): Decoder<PoolConfig> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["shieldedMinDeposit", getU64Decoder()],
    ["shieldedMaxDeposit", getU64Decoder()],
    ["backingMinDeposit", getU64Decoder()],
    ["backingMaxDeposit", getU64Decoder()],
    ["maxTvlUsd", getU128Decoder()],
    ["minimumPoolTime", getU64Decoder()],
    ["unlockDuration", getU64Decoder()],
    ["protocolFeeBp", getU64Decoder()],
    ["protocolFeeRecipient", getAddressDecoder()],
    ["poolFeeRecipient", getAddressDecoder()],
    ["priceOracle", getAddressDecoder()],
    ["accessControl", getOptionDecoder(getAddressDecoder())],
    ["shieldTransferLock", getU64Decoder()],
    ["protectorTransferLock", getU64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link PoolConfig} account data. */
export function getPoolConfigCodec(): Codec<PoolConfigArgs, PoolConfig> {
  return combineCodec(getPoolConfigEncoder(), getPoolConfigDecoder());
}

export function decodePoolConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<PoolConfig, TAddress>;
export function decodePoolConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<PoolConfig, TAddress>;
export function decodePoolConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<PoolConfig, TAddress> | MaybeAccount<PoolConfig, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getPoolConfigDecoder(),
  );
}

export async function fetchPoolConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<PoolConfig, TAddress>> {
  const maybeAccount = await fetchMaybePoolConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePoolConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<PoolConfig, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePoolConfig(maybeAccount);
}

export async function fetchAllPoolConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<PoolConfig>[]> {
  const maybeAccounts = await fetchAllMaybePoolConfig(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePoolConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<PoolConfig>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePoolConfig(maybeAccount));
}
