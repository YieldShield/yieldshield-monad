/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const WHITELIST_ENTRY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [51, 70, 173, 81, 219, 192, 234, 62],
);

export function getWhitelistEntryDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WHITELIST_ENTRY_DISCRIMINATOR,
  );
}

export type WhitelistEntry = {
  discriminator: ReadonlyUint8Array;
  /** Display name (≤ `MAX_TOKEN_NAME_LENGTH`). */
  name: string;
  /** Symbol (≤ `MAX_TOKEN_SYMBOL_LENGTH`, CON-107). */
  symbol: string;
  /** The token mint this entry whitelists. */
  mint: Address;
  /** Token decimals (CON-107 range [6, 32] enforced at add time). */
  decimals: number;
  /** Primary oracle feed account (in `composite-oracle`) for this token. */
  primaryFeed: Address;
  /** Optional backup oracle feed account (dual-feed). */
  backupFeed: Option<Address>;
  /** Minimum collateral ratio (bps) permitted for pools backed by this token. */
  minCollateralRatioBp: bigint;
  /** When true, the token must expose a strict protected-price path (SEC-006). */
  requiresStrictProtectedPrice: boolean;
  /** PDA bump. */
  bump: number;
};

export type WhitelistEntryArgs = {
  /** Display name (≤ `MAX_TOKEN_NAME_LENGTH`). */
  name: string;
  /** Symbol (≤ `MAX_TOKEN_SYMBOL_LENGTH`, CON-107). */
  symbol: string;
  /** The token mint this entry whitelists. */
  mint: Address;
  /** Token decimals (CON-107 range [6, 32] enforced at add time). */
  decimals: number;
  /** Primary oracle feed account (in `composite-oracle`) for this token. */
  primaryFeed: Address;
  /** Optional backup oracle feed account (dual-feed). */
  backupFeed: OptionOrNullable<Address>;
  /** Minimum collateral ratio (bps) permitted for pools backed by this token. */
  minCollateralRatioBp: number | bigint;
  /** When true, the token must expose a strict protected-price path (SEC-006). */
  requiresStrictProtectedPrice: boolean;
  /** PDA bump. */
  bump: number;
};

/** Gets the encoder for {@link WhitelistEntryArgs} account data. */
export function getWhitelistEntryEncoder(): Encoder<WhitelistEntryArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["symbol", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["mint", getAddressEncoder()],
      ["decimals", getU8Encoder()],
      ["primaryFeed", getAddressEncoder()],
      ["backupFeed", getOptionEncoder(getAddressEncoder())],
      ["minCollateralRatioBp", getU64Encoder()],
      ["requiresStrictProtectedPrice", getBooleanEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: WHITELIST_ENTRY_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link WhitelistEntry} account data. */
export function getWhitelistEntryDecoder(): Decoder<WhitelistEntry> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["symbol", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["mint", getAddressDecoder()],
    ["decimals", getU8Decoder()],
    ["primaryFeed", getAddressDecoder()],
    ["backupFeed", getOptionDecoder(getAddressDecoder())],
    ["minCollateralRatioBp", getU64Decoder()],
    ["requiresStrictProtectedPrice", getBooleanDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link WhitelistEntry} account data. */
export function getWhitelistEntryCodec(): Codec<
  WhitelistEntryArgs,
  WhitelistEntry
> {
  return combineCodec(getWhitelistEntryEncoder(), getWhitelistEntryDecoder());
}

export function decodeWhitelistEntry<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<WhitelistEntry, TAddress>;
export function decodeWhitelistEntry<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<WhitelistEntry, TAddress>;
export function decodeWhitelistEntry<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<WhitelistEntry, TAddress> | MaybeAccount<WhitelistEntry, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getWhitelistEntryDecoder(),
  );
}

export async function fetchWhitelistEntry<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<WhitelistEntry, TAddress>> {
  const maybeAccount = await fetchMaybeWhitelistEntry(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeWhitelistEntry<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<WhitelistEntry, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeWhitelistEntry(maybeAccount);
}

export async function fetchAllWhitelistEntry(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<WhitelistEntry>[]> {
  const maybeAccounts = await fetchAllMaybeWhitelistEntry(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeWhitelistEntry(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<WhitelistEntry>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeWhitelistEntry(maybeAccount),
  );
}
