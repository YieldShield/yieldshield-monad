/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const SHIELD_POSITION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [126, 138, 231, 79, 41, 163, 216, 117],
);

export function getShieldPositionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SHIELD_POSITION_DISCRIMINATOR,
  );
}

export type ShieldPosition = {
  discriminator: ReadonlyUint8Array;
  /** The pool this position belongs to. */
  pool: Address;
  /** Shielded-token amount (native units). */
  amount: bigint;
  /** Entry timestamp (drives transfer lock; `i64` per `Clock::unix_timestamp`). */
  depositTime: bigint;
  /** USD value at entry, 8-decimal (basis for collateral sizing and fee high-water-mark). */
  valueAtDeposit: bigint;
  /** Backing-token collateral cap attributable to this position. */
  collateralAmount: bigint;
  /** Last fee-crystallization timestamp. */
  lastFeeClaimTime: bigint;
  /** SEC-105 per-position USD fee high-water-mark (already-taxed yield floor). */
  feeValueBaselineUsd: bigint;
  /** Last `claim_rewards` timestamp (REQ-006 `CLAIM_REWARDS_COOLDOWN`). */
  lastClaimRewardsTime: bigint;
  /** PDA bump. */
  bump: number;
};

export type ShieldPositionArgs = {
  /** The pool this position belongs to. */
  pool: Address;
  /** Shielded-token amount (native units). */
  amount: number | bigint;
  /** Entry timestamp (drives transfer lock; `i64` per `Clock::unix_timestamp`). */
  depositTime: number | bigint;
  /** USD value at entry, 8-decimal (basis for collateral sizing and fee high-water-mark). */
  valueAtDeposit: number | bigint;
  /** Backing-token collateral cap attributable to this position. */
  collateralAmount: number | bigint;
  /** Last fee-crystallization timestamp. */
  lastFeeClaimTime: number | bigint;
  /** SEC-105 per-position USD fee high-water-mark (already-taxed yield floor). */
  feeValueBaselineUsd: number | bigint;
  /** Last `claim_rewards` timestamp (REQ-006 `CLAIM_REWARDS_COOLDOWN`). */
  lastClaimRewardsTime: number | bigint;
  /** PDA bump. */
  bump: number;
};

/** Gets the encoder for {@link ShieldPositionArgs} account data. */
export function getShieldPositionEncoder(): FixedSizeEncoder<ShieldPositionArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["pool", getAddressEncoder()],
      ["amount", getU64Encoder()],
      ["depositTime", getI64Encoder()],
      ["valueAtDeposit", getU128Encoder()],
      ["collateralAmount", getU64Encoder()],
      ["lastFeeClaimTime", getI64Encoder()],
      ["feeValueBaselineUsd", getU128Encoder()],
      ["lastClaimRewardsTime", getI64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SHIELD_POSITION_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link ShieldPosition} account data. */
export function getShieldPositionDecoder(): FixedSizeDecoder<ShieldPosition> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["pool", getAddressDecoder()],
    ["amount", getU64Decoder()],
    ["depositTime", getI64Decoder()],
    ["valueAtDeposit", getU128Decoder()],
    ["collateralAmount", getU64Decoder()],
    ["lastFeeClaimTime", getI64Decoder()],
    ["feeValueBaselineUsd", getU128Decoder()],
    ["lastClaimRewardsTime", getI64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link ShieldPosition} account data. */
export function getShieldPositionCodec(): FixedSizeCodec<
  ShieldPositionArgs,
  ShieldPosition
> {
  return combineCodec(getShieldPositionEncoder(), getShieldPositionDecoder());
}

export function decodeShieldPosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ShieldPosition, TAddress>;
export function decodeShieldPosition<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ShieldPosition, TAddress>;
export function decodeShieldPosition<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<ShieldPosition, TAddress> | MaybeAccount<ShieldPosition, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getShieldPositionDecoder(),
  );
}

export async function fetchShieldPosition<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ShieldPosition, TAddress>> {
  const maybeAccount = await fetchMaybeShieldPosition(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeShieldPosition<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ShieldPosition, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeShieldPosition(maybeAccount);
}

export async function fetchAllShieldPosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ShieldPosition>[]> {
  const maybeAccounts = await fetchAllMaybeShieldPosition(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeShieldPosition(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ShieldPosition>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeShieldPosition(maybeAccount),
  );
}

export function getShieldPositionSize(): number {
  return 113;
}
