/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const FACTORY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  159, 68, 192, 61, 48, 249, 216, 202,
]);

export function getFactoryDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(FACTORY_DISCRIMINATOR);
}

export type Factory = {
  discriminator: ReadonlyUint8Array;
  /** Current governance authority (`has_one` target for `onlyGovernance`; gov SEC-001/003). */
  governanceAuthority: Address;
  /** Pending authority during a two-step transfer; `None` when no transfer is in flight. */
  pendingGovernanceAuthority: Option<Address>;
  /** Default `composite-oracle` config/program used when a pool does not override it. */
  defaultOracle: Address;
  /** Total pools ever created (monotonic; never decremented). */
  poolCount: bigint;
  /** Currently active pools (≤ `max_active_pools`). */
  activePoolCount: number;
  /** CON-109 ceiling on simultaneously active pools. */
  maxActivePools: number;
  /** CON-109 minimum creation bond, USD 8-decimal. */
  minimumCreationBondUsd: bigint;
  /** Number of whitelisted tokens (≤ `MAX_WHITELISTED_TOKENS`). */
  whitelistedTokenCount: number;
  /** True once governance bootstrap has been finalized (one-way). */
  bootstrapFinalized: boolean;
  /** Global pause flag (gov pause; blocks deposits/most withdrawals). */
  paused: boolean;
  /** PDA bump. */
  bump: number;
};

export type FactoryArgs = {
  /** Current governance authority (`has_one` target for `onlyGovernance`; gov SEC-001/003). */
  governanceAuthority: Address;
  /** Pending authority during a two-step transfer; `None` when no transfer is in flight. */
  pendingGovernanceAuthority: OptionOrNullable<Address>;
  /** Default `composite-oracle` config/program used when a pool does not override it. */
  defaultOracle: Address;
  /** Total pools ever created (monotonic; never decremented). */
  poolCount: number | bigint;
  /** Currently active pools (≤ `max_active_pools`). */
  activePoolCount: number;
  /** CON-109 ceiling on simultaneously active pools. */
  maxActivePools: number;
  /** CON-109 minimum creation bond, USD 8-decimal. */
  minimumCreationBondUsd: number | bigint;
  /** Number of whitelisted tokens (≤ `MAX_WHITELISTED_TOKENS`). */
  whitelistedTokenCount: number;
  /** True once governance bootstrap has been finalized (one-way). */
  bootstrapFinalized: boolean;
  /** Global pause flag (gov pause; blocks deposits/most withdrawals). */
  paused: boolean;
  /** PDA bump. */
  bump: number;
};

/** Gets the encoder for {@link FactoryArgs} account data. */
export function getFactoryEncoder(): Encoder<FactoryArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["governanceAuthority", getAddressEncoder()],
      ["pendingGovernanceAuthority", getOptionEncoder(getAddressEncoder())],
      ["defaultOracle", getAddressEncoder()],
      ["poolCount", getU64Encoder()],
      ["activePoolCount", getU32Encoder()],
      ["maxActivePools", getU32Encoder()],
      ["minimumCreationBondUsd", getU128Encoder()],
      ["whitelistedTokenCount", getU16Encoder()],
      ["bootstrapFinalized", getBooleanEncoder()],
      ["paused", getBooleanEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: FACTORY_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Factory} account data. */
export function getFactoryDecoder(): Decoder<Factory> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["governanceAuthority", getAddressDecoder()],
    ["pendingGovernanceAuthority", getOptionDecoder(getAddressDecoder())],
    ["defaultOracle", getAddressDecoder()],
    ["poolCount", getU64Decoder()],
    ["activePoolCount", getU32Decoder()],
    ["maxActivePools", getU32Decoder()],
    ["minimumCreationBondUsd", getU128Decoder()],
    ["whitelistedTokenCount", getU16Decoder()],
    ["bootstrapFinalized", getBooleanDecoder()],
    ["paused", getBooleanDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Factory} account data. */
export function getFactoryCodec(): Codec<FactoryArgs, Factory> {
  return combineCodec(getFactoryEncoder(), getFactoryDecoder());
}

export function decodeFactory<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Factory, TAddress>;
export function decodeFactory<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Factory, TAddress>;
export function decodeFactory<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Factory, TAddress> | MaybeAccount<Factory, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getFactoryDecoder(),
  );
}

export async function fetchFactory<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Factory, TAddress>> {
  const maybeAccount = await fetchMaybeFactory(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeFactory<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Factory, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeFactory(maybeAccount);
}

export async function fetchAllFactory(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Factory>[]> {
  const maybeAccounts = await fetchAllMaybeFactory(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeFactory(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Factory>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeFactory(maybeAccount));
}
